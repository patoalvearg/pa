name: CI
on:
  push:

jobs:

  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5.0.0

      - name: instalar paquetes
        run: |
          chmod +x script.sh
          ./script.sh



